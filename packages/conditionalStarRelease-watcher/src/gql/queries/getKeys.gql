query getKeys($blockHash: String!, $contractAddress: String!, $_point: Int!){
    getKeys(blockHash: $blockHash, contractAddress: $contractAddress, _point: $_point){
        value
        proof{
            data
        }
    }
}
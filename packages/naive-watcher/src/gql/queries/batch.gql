query batchData($blockHash: String!, $contractAddress: String!){
    batchData(blockHash: $blockHash, contractAddress: $contractAddress){
        value
        proof{
            data
        }
    }
}
